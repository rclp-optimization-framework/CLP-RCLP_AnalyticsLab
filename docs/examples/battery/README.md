# Battery Examples

Sample inputs for battery workflows used by the backend. Place JSON files here to use them with `POST /bateria/`.

Files:
- `battery-example-01.json` — Baseline urban corridor (gecode)
- `battery-example-02.json` — Variant (chuffed)
